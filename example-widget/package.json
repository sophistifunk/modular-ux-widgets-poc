{
  "name": "example-widget",
  "version": "1.0.0",
  "description": "Example Widget for PoC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "tsc": "rm -rf dist && tsc",
    "tsc:watch": "rm -rf dist && tsc -w",
    "dist": "rm -rf dist && tsc && node dist-package.js",
    "prepublishOnly": "echo \"Do not publish from root, publish from dist\" && exit 1"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/react": "16.4.2",
    "@types/react-dom": "16.0.6",
    "typescript": "^2.9.2"
  },
  "peerDependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  }
}
